when user types first prompt (or there are exisitng prompts but they all not successful), we want to AI analyse the prompt first. Usualy the promopts are vague so we want to clarify main things, epecially around design. We should treat the user if they are not technical and they are not designers so they might not know what specific fonts are. We want to come up with max 3 questions with up to 3 options (plus a free text field Type Something) each. User choses their preferences and only after that the prompt executes. We also want to give them a chance to skip these question by proviidng aletrnative button called "Surprise Me" where AI makes best judgement