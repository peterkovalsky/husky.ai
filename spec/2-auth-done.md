Add Supabase auth;
- Create a SignIn/SignUp page where user can login or sign up with email and password
- Add forgot password functionality
- For signup, we need the extra field Name at the top that we will save into DisplayName field in supabase auth
